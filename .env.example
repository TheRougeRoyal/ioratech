# MongoDB connection
MONGO_URL=mongodb://localhost:27017
DB_NAME=ioratech

# Public base URL (required for absolute URLs in production)
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

# CORS allowed origins (defaults to * if not set)
CORS_ORIGINS=*
